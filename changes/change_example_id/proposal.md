# <Verb-led title: what changes, not how>

## Why

The problem in terms of customer or operational impact. Evidence required:
`file.ts:LINE`, a failing test, a measurement, or a business requirement
reference (docs number, client requirement).

## What Changes

- Observable difference one.
- Observable difference two.

## Impact

Affected capabilities (`specs/<capability>/`), affected code, migration and
rollback considerations, and what breaks if we do nothing.
